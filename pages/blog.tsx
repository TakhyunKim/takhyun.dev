import type { NextPage } from "next";

import Container from "../components/Containers";

const Blog: NextPage = () => {
  return (
    <Container>
      <span>Blog</span>
    </Container>
  );
};

export default Blog;
